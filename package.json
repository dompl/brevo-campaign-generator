{
	"name": "brevo-campaign-generator",
	"version": "1.0.0",
	"description": "WooCommerce plugin to generate Brevo email campaigns using AI.",
	"private": true,
	"author": "Red Frog Studio <hello@redfrogstudio.co.uk>",
	"license": "UNLICENSED",
	"scripts": {
		"lint": "eslint admin/js/*.js",
		"lint:fix": "eslint admin/js/*.js --fix",
		"build": "echo 'No build step required — plain JS and CSS.'",
		"test": "echo 'No tests specified' && exit 0"
	},
	"devDependencies": {
		"eslint": "^8.0.0"
	},
	"engines": {
		"node": ">=18.0.0"
	}
}
